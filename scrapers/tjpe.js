// Scraper TJPE — sistema próprio (não eSAJ)
// TODO Fase 4: implementar usando https://www.tjpe.jus.br/jurisprudencia ou similar.
// TJPE não usa eSAJ, tem portal próprio. Mapear endpoint via DevTools.

export async function tjpe(_browser, { busca }) {
  console.log(`[tjpe] TODO scraper não implementado (busca: "${busca}")`);
  return [];
}
