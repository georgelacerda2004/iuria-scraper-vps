// Scraper TST — Jurisprudência TST
// TODO Fase 4: implementar usando https://jurisprudencia.tst.jus.br/
// Endpoint API mudou (HTTP 405 no antigo). Mapear novo endpoint via DevTools
// na navegação real, ou ler DOM final após XHR resolver.

export async function tst(_browser, { busca }) {
  console.log(`[tst] TODO scraper não implementado (busca: "${busca}")`);
  return [];
}
